# Response: 2026-06-18T09:30:45.360873

Done. The SPA now ignores any legacy gems dot json files and only reads per gem objects. The deletable flag and its UI guards are gone, so every row gets a working delete button. After you remove the legacy file from the bucket, the registry will show exactly your three gems.
